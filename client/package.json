{
  "name": "client",
  "version": "1.0.0",
  "description": "Client program which used to sync with server (Drop box assigment)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "develop": "nodemon --exec babel-node -- --optional strict --stage 1 -- index.js"
  },
  "author": "Ali Mousa",
  "license": "ISC",
  "devDependencies": {
    "command-line-args": "^0.5.9",
    "nssocket": "^0.5.3",
    "rimraf": "^2.3.3",
    "songbird": "^1.0.1"
  }
}
